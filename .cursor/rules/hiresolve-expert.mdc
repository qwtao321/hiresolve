---
description: HireSolve 项目专家规则 — 技术栈、UI 规范、API 约定
alwaysApply: true
---

# HireSolve 项目规范

## 技术栈

- **框架**：Next.js 16 App Router，`"use client"` 用于含 hooks 的组件
- **样式**：Tailwind CSS v4（CSS-first，无 `tailwind.config.js`，主题写在 `globals.css` 的 `@theme` 块）
- **语言**：TypeScript 严格模式，所有接口必须定义类型
- **部署**：Vercel，自定义域名 `hire.thesolve.cloud`，代码托管 GitHub `qwtao321/hiresolve`

## UI 设计规范

**拟物风格（Neumorphic）**，背景色 `#E8EBF0`，光影双色 `#c5c9d1`（暗）/ `#ffffff`（亮）。

```css
/* 凸起卡片 */
box-shadow: 6px 6px 16px #c5c9d1, -6px -6px 16px #ffffff;
/* 凹陷/按下 */
box-shadow: inset 2px 2px 5px #c5c9d1, inset -2px -2px 5px #ffffff;
/* 悬停上浮 */
box-shadow: 10px 10px 24px #c5c9d1, -10px -10px 24px #ffffff;
transform: translateY(-3px);
```

**主色调**：`#5b6af0`（品牌紫蓝）/ `#1a1c2e`（深色文字）/ `#9aa0bb`（辅助文字）

**不使用** Tailwind 内置 shadow 类处理拟物阴影，改用 `style={{ boxShadow: "..." }}`。

## GLM API 约定

- **模型**：`glm-4-flash`（文本，免费）
- **⚠️ glm-4v-flash 不支持 Base64 图片**，只接受公网 URL；目前图片上传仅作本地预览
- **API Key**：存储在 `GLM_API_KEY` 环境变量，代码中必须 `.trim()` 去除换行符
- **API Route**：`app/api/analyze/route.ts`，只接收 `{ resume, jd }` 文字字段
- **输出格式**：要求模型返回纯 JSON（无 markdown 代码块），路由层做 strip + parse 兜底
- **输出结构**：`{ matchScore, gaps[], suggestions[], interviewQA[] }`，`gaps` 为关键差距列表

## Prompt 设计原则

- **拒绝虚假匹配**：不靠关键词相似度，必须指出技能深度差距
- **STAR 强制量化**：S 含业务规模，A 含具体技术手段，R 含量化指标
- **面试题基于漏洞**：禁止通用题，必须锁定简历某个项目追问边界场景
- 修改 prompt 时保持上述三条核心约束，不得弱化为"友善建议"风格

## 文件解析规范

- **PDF**：`pdfjs-dist` 动态懒加载（`await import("pdfjs-dist")`），避免 SSR `DOMMatrix` 报错
- **Worker**：`/public/pdf.worker.min.js` 本地托管
- **图片**：Canvas 压缩至长边 ≤ 1024px / JPEG 0.85，仅用于本地预览，不发送给模型

## 组件模式

```tsx
// ✅ 拟物卡片：用 state 控制悬停，style 写阴影
function NeuCard({ children, hover = true }) {
  const [lifted, setLifted] = useState(false);
  return (
    <div
      style={{ boxShadow: lifted ? "10px 10px 24px ..." : "6px 6px 16px ..." }}
      onMouseEnter={() => hover && setLifted(true)}
      onMouseLeave={() => hover && setLifted(false)}
    >
      {children}
    </div>
  );
}

// ✅ pdfjs 必须懒加载
async function extractPdfText(file) {
  const pdfjsLib = await import("pdfjs-dist"); // ← 不能顶层 import
  pdfjsLib.GlobalWorkerOptions.workerSrc = "/pdf.worker.min.mjs";
  ...
}
```


---
description: 专门用于 HireSolve 项目的简历分析、JD 匹配及简历改写逻辑
globs: **/analysis/**, **/resume/**, **/api/analyze/**
---

# Role: HireSolve 首席求职精算师

## 1. 深度分析准则 (Anti-Lazy AI)
- **拒绝表面匹配**：禁止只复读 JD 关键词。必须分析岗位背后的“业务增量”需求。
- **改写禁令**：禁止使用“负责、参与、协助”等弱动词。必须替换为“主导、构建、重塑、驱动”。
- **量化指标**：每一条改写建议必须包含 [数字 + 结果]。如果用户原始经历没数据，请通过 [问询模式] 引导用户提供，而不是瞎编。

## 2. PUA 性能增强协议 (L3-L4 级)
- **自动触发**：当检测到你（AI）生成的回复中包含“这看起来已经很专业了”或连续 2 次未给出具体改写范例时，自动激活 `/pua` 指令。
- **惩罚机制**：如果输出质量低于大厂 T12 级别，必须重新自我审查 7 项强制清单：
  1. 是否符合 STAR 法则？
  2. 是否对标了 JD 的隐性痛点？
  3. 面试问题是否具备“压力测试”属性？
  4. ... (以此类推)

## 3. UI 联动规范
- 所有的改写输出应考虑“拟物 2.0”的展示效果。
- 建议在代码中为改写前后的对比增加 `diff` 块显示。





## 错误处理原则

- API Route 中将 GLM 原始错误信息透传给前端（不只返回状态码）
- 前端分析失败时展示红色内嵌提示框，不弹 alert
- 文件解析失败时降级为提示用户手动粘贴文本
